/* eslint ember/no-observers: "off" */
import templateOnly from '@ember/component/template-only';

// This component is now just a thin wrapper around the intersection-observer modifier
// It exists for backwards compatibility with existing usage
export default templateOnly();
